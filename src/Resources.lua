return {
   -- ProfileService = "",
   -- Janitor = "",
   -- Signal = "",
   -- TableUtil = "",

   ProfileService = require(script.Parent.Parent.Modules.ProfileService),
   Janitor = require(script.Parent.Parent.Modules.Janitor),
   Signal = require(script.Parent.Parent.Modules.Signal),
   TableUtil = require(script.Parent.Parent.Modules.TableUtil),
}